Use `try_crit_script` to inspect the nodes available.<br>
Note: Graphs are currently unavailable through `try_crit_script`.

See `test_core_library` and `test_make` to see the current state of Crit-Script.

Crit-Script is currently in development.
