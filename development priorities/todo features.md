# GUI features
* Drag off of a pin and show a list of functions... type in the name to narrow the query.
  * Add scrolling to go through the list :P
  * Note: don't add super advanced query features. Your audience is unlikely to use them. If they _do_, however, you'd want:
    * the -"..." to say "does not include anything within the quotations"
    * include a type:<node_type> or -type:<node_type>
    * include a has_out:<pin_type> or -has_out:<pin_type>

# User-creatable objects
* Events (in Python)
* Events (in CritScript)
* Structs
* "Enumerations"
* ~~State~~ Very low priority, I think.